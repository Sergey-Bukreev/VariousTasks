import React from 'react';
import classes from "./Header.module.css"

export const Header:React.FC = () => {
    return (
        <header className={classes.header}>
            Hello it my Page for Training Tasks
        </header>
    );
};

